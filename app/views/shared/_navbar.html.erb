<nav class="navbar navbar-expand-lg bg-body-tertiary rounded border border-info">
      <ul class="navbar-nav mx-auto mb-2 mb-lg-0">
        <li class="nav-item dropdown btn btn-lg">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            <span class="dropdown-label"><%= t 'select_city' %></span>
          </a>
          <ul class="dropdown-menu" data-controller="cities">
            <%- @cities.each do |city| %>
              <li><a class="dropdown-item" data-lat="<%= city.lat %>" data-long="<%= city.long %>" data-timezone="<%= city.timezone %>" data-action="cities#select"><%= city.name %></a></li>
            <% end %>
          </ul>
        </li>
      </ul>
</nav>
